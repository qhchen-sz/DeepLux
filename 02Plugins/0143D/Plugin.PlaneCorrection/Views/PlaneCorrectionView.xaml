<vb:ModuleViewBase x:Class="Plugin.PlaneCorrection.Views.PlaneCorrectionView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:vb="clr-namespace:HV.Core;assembly=HV"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:Plugin.PlaneCorrection.Views"
                   xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:cvt="clr-namespace:HV.Assets.Converter;assembly=HV"
                   xmlns:vm="clr-namespace:Plugin.PlaneCorrection.ViewModels"
                   xmlns:WinFormHost="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
                   Height="680" Width="980"
                   Loaded="ModuleViewBase_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <cvt:EnumConverter x:Key="EnumConverter"/>
                    <cvt:StatusConverter x:Key="StatusConverter"/>
                    <vm:EnumDescConverter x:Key="EnumDescConverter"/>
                </ResourceDictionary>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Collection.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TabControl Style="{StaticResource VerTabControl}">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource VerTabItem}"/>
                </TabControl.Resources>
                <TabItem Header="基本参数">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                        <Grid x:Name="gd">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="43*"/>
                                <ColumnDefinition Width="114*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.ColumnSpan="2">
                                <GroupBox Header="图像链接">
                                    <StackPanel Orientation="Horizontal" Margin="5 0">
                                        <TextBlock Text="输入图像" VerticalAlignment="Center"/>
                                        <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0"
                                                 BorderThickness="0 0 0 1" IsReadOnly="True"
                                                 Text="{Binding InputImageLinkText}"
                                                 mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                 mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InputImageLink}"
                                                 mah:TextBoxHelper.Watermark="HImage"/>
                                    </StackPanel>
                                </GroupBox>
                            </Border>
                            <Border Grid.Row="1" Grid.ColumnSpan="2">
                                <GroupBox Header="参数设置">
                                    <StackPanel>
                                        <GroupBox Header="校正方式" Margin="5" Padding="5">
                                            <StackPanel Orientation="Horizontal">
                                                <RadioButton Content="快速校正" GroupName="CorrectionMode" Margin="0 2 8 2"
                                                             IsChecked="{Binding CorrectionMode, Converter={StaticResource EnumConverter}, ConverterParameter={x:Static vm:eCorrectionMode.Quick}}"/>
                                                <RadioButton Content="投影校正" GroupName="CorrectionMode" Margin="0 2 8 2"
                                                             IsChecked="{Binding CorrectionMode, Converter={StaticResource EnumConverter}, ConverterParameter={x:Static vm:eCorrectionMode.Projection}}"/>
                                                <RadioButton Content="点到面距离图" GroupName="CorrectionMode" Margin="0 2"
                                                             IsChecked="{Binding CorrectionMode, Converter={StaticResource EnumConverter}, ConverterParameter={x:Static vm:eCorrectionMode.PointToPlaneDistance}}"/>
                                            </StackPanel>
                                        </GroupBox>
                                        <GroupBox Header="分辨率设置" Margin="5" Padding="5">
                                            <GroupBox.Style>
                                                <Style TargetType="GroupBox">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CorrectionMode}" Value="{x:Static vm:eCorrectionMode.Projection}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </GroupBox.Style>
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <TextBlock Text="X分辨率:" VerticalAlignment="Center" Width="70"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="190" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitResolutionX.Text}"
                                                             IsReadOnly="{Binding InitResolutionX.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitResolutionX.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitResolutionX}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <TextBlock Text="Y分辨率:" VerticalAlignment="Center" Width="70"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="190" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitResolutionY.Text}"
                                                             IsReadOnly="{Binding InitResolutionY.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitResolutionY.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitResolutionY}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <TextBlock Text="Z分辨率:" VerticalAlignment="Center" Width="70"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="190" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitResolutionZ.Text}"
                                                             IsReadOnly="{Binding InitResolutionZ.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitResolutionZ.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitResolutionZ}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </GroupBox>
                                        <GroupBox Header="基准平面参数" Margin="5" Padding="5">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <RadioButton Content="引用平面图像"
                                                                 IsChecked="{Binding PlaneMode, Converter={StaticResource EnumConverter}, ConverterParameter={x:Static vm:ePlaneMode.PlaneImage}}"/>
                                                    <RadioButton Content="手动输入参数" Margin="10 0 0 0"
                                                                 IsChecked="{Binding PlaneMode, Converter={StaticResource EnumConverter}, ConverterParameter={x:Static vm:ePlaneMode.Manual}}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <StackPanel.Style>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PlaneMode}" Value="{x:Static vm:ePlaneMode.Manual}">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </StackPanel.Style>
                                                    <TextBlock Text="平面图像:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding PlaneImageLinkText}"
                                                             IsReadOnly="True"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.PlaneImageLink}"
                                                             mah:TextBoxHelper.Watermark="HImage"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <StackPanel.Style>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PlaneMode}" Value="{x:Static vm:ePlaneMode.Manual}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </StackPanel.Style>
                                                    <TextBlock Text="Nx:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitNx.Text}"
                                                             IsReadOnly="{Binding InitNx.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitNx.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitNx}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <StackPanel.Style>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PlaneMode}" Value="{x:Static vm:ePlaneMode.Manual}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </StackPanel.Style>
                                                    <TextBlock Text="Ny:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitNy.Text}"
                                                             IsReadOnly="{Binding InitNy.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitNy.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitNy}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <StackPanel.Style>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PlaneMode}" Value="{x:Static vm:ePlaneMode.Manual}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </StackPanel.Style>
                                                    <TextBlock Text="Nz:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitNz.Text}"
                                                             IsReadOnly="{Binding InitNz.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitNz.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitNz}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0 2">
                                                    <StackPanel.Style>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PlaneMode}" Value="{x:Static vm:ePlaneMode.Manual}">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </StackPanel.Style>
                                                    <TextBlock Text="D:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitD.Text}"
                                                             IsReadOnly="{Binding InitD.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitD.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitD}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </GroupBox>
                                        <GroupBox Header="ROI设置" Margin="5" Padding="5">
                                            <StackPanel>
                                                <CheckBox Content="使用ROI" IsChecked="{Binding UseRoi}" Margin="5"/>
                                                <StackPanel Orientation="Horizontal" Margin="5">
                                                    <TextBlock Text="中心X:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitRoiCenterX.Text}"
                                                             IsReadOnly="{Binding InitRoiCenterX.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitRoiCenterX.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitRoiCenterX}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="5">
                                                    <TextBlock Text="中心Y:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitRoiCenterY.Text}"
                                                             IsReadOnly="{Binding InitRoiCenterY.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitRoiCenterY.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitRoiCenterY}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="5">
                                                    <TextBlock Text="半长1:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitRoiLength1.Text}"
                                                             IsReadOnly="{Binding InitRoiLength1.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitRoiLength1.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitRoiLength1}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="5">
                                                    <TextBlock Text="半长2:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitRoiLength2.Text}"
                                                             IsReadOnly="{Binding InitRoiLength2.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitRoiLength2.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitRoiLength2}"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="5">
                                                    <TextBlock Text="角度:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitRoiAngel.Text}"
                                                             IsReadOnly="{Binding InitRoiAngel.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitRoiAngel.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitRoiAngel}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </GroupBox>
                                        <GroupBox Header="平移设置" Margin="5" Padding="5">
                                            <StackPanel>
                                                <CheckBox Content="启用平移" IsChecked="{Binding IsTranslateEnabled}" Margin="5"/>
                                                <StackPanel Orientation="Horizontal" Margin="5">
                                                    <StackPanel.Style>
                                                        <Style TargetType="StackPanel">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsTranslateEnabled}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </StackPanel.Style>
                                                    <TextBlock Text="平移值:" VerticalAlignment="Center" Width="60"/>
                                                    <TextBox Style="{StaticResource LinkTextBox}" Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                             Text="{Binding InitTranslateZ.Text}"
                                                             IsReadOnly="{Binding InitTranslateZ.IsReadOnly}"
                                                             mah:TextBoxHelper.Value="{Binding InitTranslateZ.Value}"
                                                             mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                             mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InitTranslateZ}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </GroupBox>
                                    </StackPanel>
                                </GroupBox>
                            </Border>
                            <Border Grid.Row="2" Grid.ColumnSpan="2">
                                <GroupBox Header="输出数据">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="平面度:" VerticalAlignment="Center" Width="70"/>
                                                <TextBox Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                         Text="{Binding Flatness, StringFormat=F6}" IsReadOnly="True"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="最大偏差:" VerticalAlignment="Center" Width="70"/>
                                                <TextBox Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                         Text="{Binding MaxDeviation, StringFormat=F6}" IsReadOnly="True"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="最小偏差:" VerticalAlignment="Center" Width="70"/>
                                                <TextBox Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                         Text="{Binding MinDeviation, StringFormat=F6}" IsReadOnly="True"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="RMS:" VerticalAlignment="Center" Width="70"/>
                                                <TextBox Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                         Text="{Binding RmsError, StringFormat=F6}" IsReadOnly="True"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <TextBlock Text="高度距离:" VerticalAlignment="Center" Width="70"/>
                                                <TextBox Width="200" Margin="5 0" BorderThickness="0 0 0 1"
                                                         Text="{Binding HeightDistance, StringFormat=F6}" IsReadOnly="True"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </ScrollViewer>
                                </GroupBox>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="显示设置">
                    <Grid x:Name="gd2">
                        <GroupBox Header="显示设置">
                            <StackPanel>
                                <CheckBox Content="显示校正图" IsChecked="{Binding ShowCorrectedImage, Mode=TwoWay}" Margin="5"/>
                                <CheckBox Content="显示区域" IsChecked="{Binding ShowRegion, Mode=TwoWay}" Margin="5"/>
                                <CheckBox Content="显示结果文字" IsChecked="{Binding ShowResultPoint, Mode=TwoWay}" Margin="5"/>
                            </StackPanel>
                        </GroupBox>
                    </Grid>
                </TabItem>
            </TabControl>
            <GridSplitter Grid.Column="1"/>
            <WinFormHost:WindowsFormsHost Margin="5" Grid.Column="2" x:Name="winFormHost"/>
        </Grid>
        <Border Grid.Row="1" BorderBrush="White" BorderThickness="0 0.5 0 0">
            <DockPanel LastChildFill="True">
                <StackPanel Margin="10 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock>
                        <Run Text="耗时:"/><Run Text="{Binding ModuleParam.ElapsedTime}"/><Run Text="ms"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="状态:"/>
                        <Run Text="{Binding ModuleParam.Status,Converter={StaticResource StatusConverter}}"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnExecute" Content="执行" Margin="10 0" Command="{Binding ExecuteCommand}"/>
                    <Button x:Name="btnCancel" Content="取消" Margin="10 0" Click="btnCancel_Click"/>
                    <Button x:Name="btnConfirm" Content="确认" Margin="10 0" Command="{Binding ConfirmCommand}"
                            Background="#FFFF2000" BorderBrush="#FFFF2000"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</vb:ModuleViewBase>
