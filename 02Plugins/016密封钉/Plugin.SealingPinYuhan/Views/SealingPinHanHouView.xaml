<vb:ModuleViewBase x:Class="Plugin.SealingPinYuhan.Views.SealingPinYuhanView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                   xmlns:vb="clr-namespace:HV.Core;assembly=HV"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                   xmlns:local="clr-namespace:Plugin.SealingPinYuhan.Views"
                   xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                   xmlns:helper="clr-namespace:HV.Common.Helper;assembly=HV"
                   xmlns:enum="clr-namespace:HV.Common.Enums;assembly=HV"
                   xmlns:vm="clr-namespace:Plugin.SealingPinYuhan.ViewModels"
                   xmlns:cvt="clr-namespace:HV.Assets.Converter;assembly=HV"
                   xmlns:WinFormHost="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
                   Icon="/Plugin.SealingPinYuhan;component/Assets/Images/Tool/SealingPinYuhan.png" 
                   xmlns:dew ="clr-namespace:Plugin.SealingPinYuhan.Views"
                   Height="680" Width="980"
                   >

    <vb:ModuleViewBase.Resources>
        <ResourceDictionary>
            <!-- 先放本地资源 -->
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <local:StrToVisibilityConverter x:Key="StrToVisibilityConverter"/>
            <!-- 合并外部资源 -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/TextBoxStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/TabControlStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/Converter.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </vb:ModuleViewBase.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="52*"/>
            <ColumnDefinition Width="311*"/>
            <ColumnDefinition Width="617*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>
        <Grid Grid.ColumnSpan="3" Margin="0,0,0,55" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="370"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TabControl Style="{StaticResource VerTabControl}">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource VerTabItem}"/>
                </TabControl.Resources>
                <TabItem Header="基本参数">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden">
                        <Grid x:Name="gd">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="97*"/>
                                <ColumnDefinition Width="245*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.ColumnSpan="2">
                                <Expander Header="图像链接" IsExpanded="True">
                                    <StackPanel HorizontalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Margin="5 0">
                                            <TextBlock Text="输入图像" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <TextBox HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="0 0 0 1" Width="200" Margin="5 0" IsReadOnly="True"
                                                 Style="{StaticResource LinkTextBox}"
                                                 Text="{Binding InputImageLinkText}"
                                                 mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                 mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InputImageLink}"/>
                                        </StackPanel>

                                    </StackPanel>
                                </Expander>
                            </Border>
                            <Border Grid.Row="1" Grid.ColumnSpan="2"  >
                                <Expander  Header="AI参数" IsExpanded="True">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="AI路径:" VerticalAlignment="Center" Width="120"/>
                                            <TextBox Width="150" Margin="5 0" BorderThickness="0 0 0 1" Text="{Binding AiPath, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" IsReadOnly="True"/>
                                            <Button Content="打开" Command="{Binding OpenAiFolder}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="CONF_THRESHOLD:" VerticalAlignment="Center"  Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding CONF_THRESHOLD, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, StringFormat=F2}" Minimum="0.01" Maximum="0.99" Interval="0.1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="NMS_THRESHOLD:" VerticalAlignment="Center"  Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding NMS_THRESHOLD, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay ,StringFormat=F2}" Minimum="0.01" Maximum="0.99" Interval="0.1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5 0">
                                            <TextBlock Text="像素当量:" VerticalAlignment="Center"  Width="120"/>
                                            <TextBox Text="{Binding Scale.Text}"
                                            mah:TextBoxHelper.Value="{Binding Scale.Value}"
                                            Style="{StaticResource LinkTextBox}"
                                            mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                            mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.Scale}"
                                            mah:TextBoxHelper.Watermark="string"
                                            VerticalAlignment="Center" HorizontalAlignment="Center" Width="200"  Margin="5 0"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <CheckBox Content="是否开机启动:" IsChecked ="{Binding IsOpen}"/>
                                            <CheckBox Content="是否显示AI结果区域" IsChecked ="{Binding IsDispAiRegion, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin=" 80,0,0,0" Checked="CheckBox_Checked"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
                                            <Button Content="打开AI" Command="{Binding AiInit}" Visibility="{Binding DeviceStatic, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>
                                            <Button Content="关闭AI" Command="{Binding AiInit}" Visibility="{Binding DeviceStatic, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>


                                    </StackPanel>

                                </Expander>

                            </Border>
                            <Border Grid.Row="2" Grid.ColumnSpan="2">
                                <Expander  Header="结果" IsExpanded="True" >
                                    <StackPanel>
                                        <StackPanel Margin="10" Orientation="Horizontal" >
                                            <TextBlock Text="钉帽圆心X：" HorizontalAlignment="Left" VerticalAlignment="Center" Width="120"/>
                                            <TextBox Text="{Binding OutSideCircleX , UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="120" IsReadOnly="True"/>
                                        </StackPanel>
                                        <StackPanel Margin="10" Orientation="Horizontal" Visibility="{Binding SelectLocation, Converter={StaticResource StrToVisibilityConverter}, ConverterParameter=Left}">
                                            <TextBlock Text="钉帽圆心Y：" HorizontalAlignment="Left" VerticalAlignment="Center" Width="120"/>
                                            <TextBox Text="{Binding OutSideCircleY , UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="120" IsReadOnly="True"/>
                                        </StackPanel>
                                        <StackPanel Margin="10" Orientation="Horizontal" Visibility="{Binding SelectLocation, Converter={StaticResource StrToVisibilityConverter}, ConverterParameter=Right}">
                                            <TextBlock Text="钉帽圆半径：" HorizontalAlignment="Left" VerticalAlignment="Center" Width="120"/>
                                            <TextBox Text="{Binding InSideCircleX , UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="120" IsReadOnly="True"/>
                                        </StackPanel>
                                        <StackPanel Margin="10" Orientation="Horizontal" Visibility="{Binding SelectLocation, Converter={StaticResource StrToVisibilityConverter}, ConverterParameter=Left}">
                                            <TextBlock Text="偏钉面积：" HorizontalAlignment="Left" VerticalAlignment="Center" Width="120"/>
                                            <TextBox Text="{Binding InSideCircleY , UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="120" IsReadOnly="True"/>
                                        </StackPanel>

                                    </StackPanel>
                                </Expander>
                            </Border>


                        </Grid>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="输出筛选">
                    <Border BorderThickness="3">
                        <StackPanel >
                            <StackPanel Margin="20" Orientation="Horizontal">
                                <CheckBox Content="是否显示中心点" IsChecked ="{Binding IsCross, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin=" 0,0,0,0" Checked="CheckBox_Checked"/>
                            </StackPanel>

                            <StackPanel Margin="20" Orientation="Horizontal">
                                <CheckBox Content="是否显示卡尺区域:" IsChecked ="{Binding IsFindRegions}"/>
                            </StackPanel>

                            <StackPanel Margin="20" Orientation="Horizontal">
                                <CheckBox Content="是否显示圆形区域:" IsChecked ="{Binding IsFindCircles}"/>
                            </StackPanel>




                        </StackPanel>
                        
                    </Border>
                    
                     
                </TabItem>
                
            </TabControl>
            <GridSplitter Grid.Column="1"/>
            <WinFormHost:WindowsFormsHost Margin="5" Grid.Column="2" x:Name="winFormHost">
            </WinFormHost:WindowsFormsHost>
        </Grid>
        <Border Grid.Row="1" BorderBrush="White" BorderThickness="0 0.5 0 0" Grid.ColumnSpan="3">
            <DockPanel LastChildFill="True">
                <StackPanel Margin="10 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock>
                        <Run Text="耗时:"/>
                        <Run Text="{Binding ModuleParam.ElapsedTime}"/>
                        <Run Text="ms"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="状态:"/>
                        <Run Text="{Binding ModuleParam.Status,Converter={StaticResource StatusConverter}}"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnExecute" Content="执行" HorizontalAlignment="Right" Margin="10 0" Command="{Binding ExecuteCommand}"/>
                    <Button x:Name="btnCancel" Content="取消" HorizontalAlignment="Right" Margin="10 0" Click="btnCancel_Click"/>
                    <Button x:Name="btnConfirm" Content="确认" HorizontalAlignment="Right" Margin="10 0" Command="{Binding ConfirmCommand}"
                            Background="#FFFF2000" BorderBrush="#FFFF2000"/>
                </StackPanel>
            </DockPanel>
        </Border>

    </Grid>
</vb:ModuleViewBase>
