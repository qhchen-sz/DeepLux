<vb:ModuleViewBase x:Class="Plugin.MeasureCalib.Views.MeasureCalibView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:vb="clr-namespace:HV.Core;assembly=HV"
                   xmlns:vm="clr-namespace:Plugin.MeasureCalib.ViewModels"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:Plugin.MeasureCalib.Views"
                   xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                   xmlns:helper="clr-namespace:HV.Common.Helper;assembly=HV"
                   xmlns:enum="clr-namespace:HV.Common.Enums;assembly=HV"
                   xmlns:cvt="clr-namespace:HV.Assets.Converter;assembly=HV"
                   xmlns:WinFormHost="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
                   xmlns:hw="clr-namespace:VM.Halcon;assembly=VM.Halcon"
                   Icon="/Plugin.MeasureCalib;component/Assets/Images/Tool/MeasureCalib.png"
                   Height="500" Width="800"
                   Loaded="ModuleViewBase_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <cvt:EnumConverter x:Key="EnumConverter"/>
                    <cvt:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
                    <cvt:StatusConverter x:Key="StatusConverter"/>
                </ResourceDictionary>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Collection.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/TextBoxStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/TabControlStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/Converter.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="5">
                    <!-- 标定模式 -->
                    <GroupBox Header="标定模式" Margin="0 0 0 5">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="5 3">
                                <TextBlock Text="模式选择" HorizontalAlignment="Center" VerticalAlignment="Center" Width="70"/>
                                <ComboBox Width="180" Height="25"
                                          ItemsSource="{Binding CalibModeDict}"
                                          DisplayMemberPath="Value"
                                          SelectedValuePath="Key"
                                          SelectedValue="{Binding CalibMode}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="5 3">
                                <TextBlock Text="相机角度" HorizontalAlignment="Center" VerticalAlignment="Center" Width="70"/>
                                <TextBox Width="180" Height="25"
                                         Style="{StaticResource LinkTextBox}"
                                         Text="{Binding CameraAngleLinkText}"
                                         mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                         mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.CameraAngleLink}"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- 像素当量设置 -->
                    <GroupBox Header="像素当量设置" Margin="0 0 0 5">
                        <StackPanel>
                            <!-- X -->
                            <StackPanel Orientation="Horizontal" Margin="5 3">
                                <TextBlock Text="X:" VerticalAlignment="Center" Width="20"/>
                                <mah:NumericUpDown Width="100" Interval="0.1"
                                                   Value="{Binding PixelEquivalentX}"/>
                            </StackPanel>
                            <!-- Y -->
                            <StackPanel Orientation="Horizontal" Margin="5 3">
                                <TextBlock Text="Y:" VerticalAlignment="Center" Width="20"/>
                                <mah:NumericUpDown Width="100" Interval="0.1"
                                                   Value="{Binding PixelEquivalentY}"/>
                            </StackPanel>
                            <!-- Z -->
                            <StackPanel Orientation="Horizontal" Margin="5 3">
                                <TextBlock Text="Z:" VerticalAlignment="Center" Width="20"/>
                                <mah:NumericUpDown Width="100" Interval="0.1"
                                                   Value="{Binding PixelEquivalentZ}"/>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
            <GridSplitter Grid.Column="1"/>
            <WinFormHost:WindowsFormsHost Margin="5" Grid.Column="2" x:Name="winFormHost">
            </WinFormHost:WindowsFormsHost>

        </Grid>
        <Border Grid.Row="1" BorderBrush="White" BorderThickness="0 0.5 0 0">
            <DockPanel LastChildFill="True">
                <StackPanel Margin="10 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock>
                        <Run Text="耗时:"/>
                        <Run Text="{Binding ModuleParam.ElapsedTime}"/>
                        <Run Text="ms"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="状态:"/>
                        <Run Text="{Binding ModuleParam.Status,Converter={StaticResource StatusConverter}}"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnExecute" Content="执行" HorizontalAlignment="Right" Margin="10 0" Command="{Binding ExecuteCommand}"/>
                    <Button x:Name="btnCancel" Content="取消" HorizontalAlignment="Right" Margin="10 0" Click="btnCancel_Click"/>
                    <Button x:Name="btnConfirm" Content="确认" HorizontalAlignment="Right" Margin="10 0" Command="{Binding ConfirmCommand}"
                            Background="#FFFF2000" BorderBrush="#FFFF2000"/>
                </StackPanel>
            </DockPanel>
        </Border>

    </Grid>
</vb:ModuleViewBase>
