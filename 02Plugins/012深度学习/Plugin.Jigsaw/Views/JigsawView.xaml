<vb:ModuleViewBase x:Class="Plugin.Jigsaw.Views.JigsawView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:vb="clr-namespace:HV.Core;assembly=HV"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:Plugin.Jigsaw.Views"
                   xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                   xmlns:helper="clr-namespace:HV.Common.Helper;assembly=HV"
                   xmlns:enum="clr-namespace:HV.Common.Enums;assembly=HV"
                   xmlns:vm="clr-namespace:Plugin.Jigsaw.ViewModels"
                   xmlns:cvt="clr-namespace:HV.Assets.Converter;assembly=HV"
                   xmlns:WinFormHost="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
                   Icon="/Plugin.Jigsaw;component/Assets/Images/Tool/Jigsaw.png"
                   xmlns:dew="clr-namespace:Plugin.Jigsaw.Views"
                   Height="550" Width="850"
                   >

    <vb:ModuleViewBase.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/TextBoxStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/TabControlStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HV;component/Assets/Styles/Converter.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </vb:ModuleViewBase.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>
        <Grid Grid.ColumnSpan="3" Margin="0,0,0,55" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="370"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TabControl Style="{StaticResource VerTabControl}">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource VerTabItem}"/>
                </TabControl.Resources>
                <TabItem Header="基本参数">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden">
                        <Grid x:Name="gd">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border>
                                <Expander Header="图像链接" IsExpanded="True">
                                    <StackPanel HorizontalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Margin="5 0">
                                            <TextBlock Text="输入图像" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <TextBox HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="0 0 0 1" Width="180" Margin="5 0" IsReadOnly="True"
                                                 Style="{StaticResource LinkTextBox}"
                                                 Text="{Binding InputImageLinkText}"
                                                 mah:TextBoxHelper.LinkButtonCommand="{Binding LinkCommand,Mode=OneWay}"
                                                 mah:TextBoxHelper.LinkButtonCommandParameter="{x:Static vm:eLinkCommand.InputImageLink}"/>
                                        </StackPanel>

                                    </StackPanel>
                                </Expander>
                            </Border>
                            <Border Grid.Row="1">
                                <Expander Header="拼图参数" IsExpanded="True">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="二值化最小值:" VerticalAlignment="Center" Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding ThresholdMin, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Minimum="0" Maximum="255" Interval="1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="二值化最大值:" VerticalAlignment="Center" Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding ThresholdMax, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Minimum="0" Maximum="255" Interval="1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="边缘厚度:" VerticalAlignment="Center" Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding EdgeThickness, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Minimum="1" Maximum="9999" Interval="10"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="分割份数:" VerticalAlignment="Center" Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding DivisionFactor, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Minimum="1" Maximum="100" Interval="1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="重叠像素:" VerticalAlignment="Center" Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding OverlapPixels, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Minimum="0" Maximum="9999" Interval="1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="输出列数:" VerticalAlignment="Center" Width="120"/>
                                            <mah:NumericUpDown Width="150" Margin="5 0" BorderThickness="0 0 0 1" Value="{Binding FinalNumCols, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Minimum="1" Maximum="100" Interval="1"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5">
                                            <TextBlock Text="启用左侧拼接:" VerticalAlignment="Center" Width="120"/>
                                            <ComboBox Width="150" Margin="5 0" BorderThickness="0 0 0 1" SelectedValuePath="Tag" SelectedValue="{Binding EnableLeftSide, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                                                <ComboBoxItem Content="禁用" Tag="0"/>
                                                <ComboBoxItem Content="启用" Tag="1"/>
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>

                                </Expander>

                            </Border>


                        </Grid>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
            <GridSplitter Grid.Column="1"/>
            <WinFormHost:WindowsFormsHost Margin="5" Grid.Column="2" x:Name="winFormHost">
            </WinFormHost:WindowsFormsHost>
        </Grid>
        <Border Grid.Row="1" BorderBrush="White" BorderThickness="0 0.5 0 0" Grid.ColumnSpan="3">
            <DockPanel LastChildFill="True">
                <StackPanel Margin="10 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock>
                        <Run Text="耗时:"/>
                        <Run Text="{Binding ModuleParam.ElapsedTime}"/>
                        <Run Text="ms"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="状态:"/>
                        <Run Text="{Binding ModuleParam.Status,Converter={StaticResource StatusConverter}}"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnExecute" Content="执行" HorizontalAlignment="Right" Margin="10 0" Command="{Binding ExecuteCommand}"/>
                    <Button x:Name="btnCancel" Content="取消" HorizontalAlignment="Right" Margin="10 0" Click="btnCancel_Click"/>
                    <Button x:Name="btnConfirm" Content="确认" HorizontalAlignment="Right" Margin="10 0" Command="{Binding ConfirmCommand}"
                            Background="#FFFF2000" BorderBrush="#FFFF2000"/>
                </StackPanel>
            </DockPanel>
        </Border>

    </Grid>
</vb:ModuleViewBase>