<UserControl x:Class="ImageControl.ImageWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border   BorderThickness="2" Background="Transparent">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path= Result ,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" Value="OK">
                        <Setter Property="BorderBrush" Value="Green"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=Result,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" Value="NG">
                        <Setter Property="BorderBrush" Value="Red"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Path=Result,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" Value="NA">
                        <Setter Property="BorderBrush" Value="White"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid Background="Black">
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <local:Meau x:Name="myMeau" Grid.Row="0"/>
            <local:OpencvWindows  x:Name="Image2D" Grid.Row="1"/>
            <local:VTKControl  x:Name="Image3D" Grid.Row="1"/>

        </Grid>
    </Border>
    
</UserControl>
