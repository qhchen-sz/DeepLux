<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:imagewindow ="clr-namespace:ImageControl;assembly=ImageControl"
        xmlns:vmhwindow ="clr-namespace:VM.Halcon;assembly=VM.Halcon"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        xmlns:chart ="clr-namespace:LiveCharts;assembly=LiveCharts"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <!--<Grid>
        <StackPanel Orientation="Horizontal">
            <ListView x:Name="sourceListView" Width="200" AllowDrop="True"
                  PreviewMouseLeftButtonDown="ListView_PreviewMouseLeftButtonDown"
                  PreviewMouseMove="ListView_PreviewMouseMove"
                  DragEnter="ListView_DragEnter"
                  Drop="ListView_Drop">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView x:Name="targetListView" Width="200" AllowDrop="True"
                  PreviewMouseLeftButtonDown="ListView_PreviewMouseLeftButtonDown"
                  PreviewMouseMove="ListView_PreviewMouseMove"
                  DragEnter="ListView_DragEnter"
                  Drop="ListView_Drop">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    -->
    <!--</Grid>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="365*"/>
            <ColumnDefinition Width="35*"/>
            <ColumnDefinition Width="36*"/>
            <ColumnDefinition Width="29*"/>
            <ColumnDefinition Width="335*"/>
        </Grid.ColumnDefinitions>
        <TreeView x:Name="uxTreeView"
	        ItemsSource="{Binding MyFooNodes}"
	        BorderThickness="0" Grid.ColumnSpan="2" Margin="0,0,0,30" Grid.RowSpan="2"/>
        <!--<imagewindow:VTKControl x:Name ="image3d" Visibility="Collapsed"/>-->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button  Click="Button_Click" Content="打开ROI" Margin="5"/>
            <Button   Content="测试" Margin="5" Click="Button_Click_1"/>
        </StackPanel>
        
        <imagewindow:ImageWindow x:Name="imagecontrol" Grid.ColumnSpan="2" Margin="0,0,0,30" Grid.RowSpan="2"/>
        <!-- 用 WindowsFormsHost 包裹 WinForms 控件！-->
        <wfi:WindowsFormsHost Grid.Column="3" Grid.ColumnSpan="2" Margin="0,0,0,30" Grid.RowSpan="2" x:Name="windows">
            <!--<vmhwindow:VMHWindowControl x:Name="vmhwindow"/>-->
        </wfi:WindowsFormsHost>

    </Grid>
</Window>
