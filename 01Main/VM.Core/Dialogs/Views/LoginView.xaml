<Window x:Class="HV.Dialogs.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="zh"
        lex:LocalizeDictionary.OutputMissingKeys="True"
        lex:ResxLocalizationProvider.DefaultAssembly="HV"
        lex:ResxLocalizationProvider.DefaultDictionary="Resource"
        xmlns:local="clr-namespace:HV.Dialogs.Views"
        xmlns:persistentData="clr-namespace:HV.PersistentData"
        mc:Ignorable="d"
        Height="450" Width="390"
        FontFamily="Microsoft YaHei" FontWeight="Normal"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen" 
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}">
    <Window.Resources>
        <ResourceDictionary>
            <persistentData:SystemConfig x:Key="systemConfig"/>
            <SolidColorBrush x:Key="TextBox.Static.Border" Color="#FFABAdB3"/>
            <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="#FF7EB4EA"/>
            <SolidColorBrush x:Key="TextBox.Focus.Border" Color="#FF569DE5"/>
            <ControlTemplate TargetType="{x:Type PasswordBox}" x:Key="PasswordBoxTemplate" >
                <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" SnapsToDevicePixels="True"
                                CornerRadius="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="&#xe6ad;" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{DynamicResource iconfont}"
                                           Foreground="#DDD"/>
                        <ScrollViewer x:Name="PART_ContentHost" Grid.Column="1"
                                              Focusable="false" 
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="Center" MinHeight="20"/>
                    </Grid>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsEnabled" Value="false">
                        <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="true">
                        <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focus.Border}"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <Border Margin="5" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect Color="Gray" ShadowDepth="0" BlurRadius="5" Opacity="1" Direction="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.618*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="{StaticResource MahApps.Brushes.AccentBase}" CornerRadius="10,10,0,0" MouseLeftButtonDown="Window_MouseLeftButtonDown">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Button VerticalAlignment="Top" HorizontalAlignment="Right" x:Name="btnExit" Cursor="Hand" Click="btnExit_Click"
                            Style="{StaticResource WindowControlButtonStyle}" Content="&#xe608;" FontFamily="{DynamicResource iconfont}"
                            />
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Border Name="bdImage" Width="100" Height="35" VerticalAlignment="Top" Margin="5 0 10 0" >
                            <Border.Background>
                                <ImageBrush ImageSource="/Dialogs/Views/Logo.jpg"/>
                            </Border.Background>
                        </Border>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Path=Ins.CompanyName, Source={StaticResource systemConfig}}"  Foreground="White" FontSize="18"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Grid.Row="1" x:Name="labTitle" Text="{lex:Loc Key=Login_UserLogin}" FontSize="24" Foreground="White" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            <Border Grid.Row="1" Background="#FF252525" CornerRadius="0,0,10,10" BorderThickness="1" BorderBrush="{StaticResource MahApps.Brushes.AccentBase}">
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1.6*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="1" Text="{lex:Loc Key=Login_Account}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                    <Border Grid.Row="1" Grid.Column="1" CornerRadius="5" SnapsToDevicePixels="True" BorderBrush="{StaticResource MahApps.Brushes.AccentBase}" 
                                    BorderThickness="1" Width="156" Height="28" Background="#FF252525">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="&#xe632;" FontFamily="{DynamicResource iconfont}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"
                                               Foreground="White"/>
                            <ComboBox Grid.Column="2" x:Name="cobUserName" Foreground="White" BorderThickness="0"
                               Background="Transparent" SelectionChanged="cobUserName_SelectionChanged" Cursor="Hand" />
                        </Grid>
                    </Border>
                    <TextBlock Grid.Row="2" Text="{lex:Loc Key=Login_Password}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                    <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="txtLoginPwd" VerticalContentAlignment="Center" BorderBrush="{StaticResource MahApps.Brushes.AccentBase}" Password="111"
                                 KeyDown="txtLoginPwd_KeyDown" Width="156" Height="28" Template="{StaticResource PasswordBoxTemplate}"/>
                    <TextBlock Grid.Row="2" Grid.Column="2" x:Name="labChangePwd" Text="{lex:Loc Key=Login_ChangePwd}" HorizontalAlignment="Left" VerticalAlignment="Center" 
                           Foreground="White" FontSize="11"  MouseEnter="labChangePwd_MouseEnter" MouseLeave="labChangePwd_MouseLeave" Cursor="Hand" MouseLeftButtonDown="labChangePwd_MouseLeftButtonDown"/>
                    <Grid Grid.Row="3" Grid.ColumnSpan="3" Grid.RowSpan="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="2" x:Name="btnLogin" Content="{lex:Loc Key=Login_Login}"
                            Click="btnLogin_Click" Cursor="Hand"/>
                        <Button Grid.Column="1" x:Name="btnLogout" Content="{lex:Loc Key=Login_Logout}"
                            Background="#FFFF2000" BorderBrush="#FFFF2000"
                            Click="btnLogout_Click"/>
                    </Grid>

                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
